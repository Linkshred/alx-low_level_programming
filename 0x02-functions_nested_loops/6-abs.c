#include "main.h"
/**
 * _abs - Compute absolute value of an integer
 * @int: The integer in question
 * Return: The absolute value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
